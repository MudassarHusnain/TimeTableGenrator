<%= form_with(model: [department,slot],url: department_slots_path(department)) do |form| %>
  <% if slot.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(slot.errors.count, "error") %> prohibited this slot from being saved:</h2>

      <ul>
        <% slot.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  


  <section class="ms-5 ps-5">
    <div class="container">
      <div class="signup-content">
        <div class="signup-form">
          <h2 class="form-title">Create New Room</h2>
          <form method="POST" class="register-form" id="register-form">
            <div class="form-group">
              <label for="name"><i class="zmdi zmdi-account material-icons-name fs-5"></i></label>
              <div class="form-group">
                <%#= form.label :courseName, style: "display: block" %>
                <%= form.time_field :start_time,placeholder: "Start Time",class: "fs-5"%>
              </div>
              <div class="form-group">
                <%#= form.label :courseName, style: "display: block" %>
                <%= form.time_field :end_time,placeholder: "End Time",class: "fs-5"%>
              </div>
              <div class="form-group form-button">
                <%= form.submit "Create Slot",class: "form-submit fs-5" %>
              </div>


            </div>
          </form>
          <div>
            <%= link_to "Back to Slots", department_slots_path %>
          </div>
        </div>
      </div>
    </div>

  </section>
<% end %>
