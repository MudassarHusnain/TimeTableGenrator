<!--<p style="color: green"><%#= notice %></p>-->

<!--<h1>Rooms</h1>-->

<!--<div id="rooms">-->
<%# @rooms.each do |room| %>
<%#= render room %>
<!--    <p>-->
<%#= link_to "Show this room", department_room_path(room.department_id,room) %>
<!--    </p>-->
<%# end %>
<!--</div>-->

<%#= link_to "New room", new_department_room_path %>
<div class="container" style="margin-top: 20%;margin-left: 20%;width: 100%">
  <div class="row justify-content-center mt-4">
    <div class="col-auto">
    <%= link_to "Teachers", department_teachers_path(@department), class: "btn btn-primary" %>
  </div>
  <div class="col-auto">
    <%= link_to "Courses", department_courses_path(@department), class: "btn btn-primary" %>
  </div>
  <% if current_user.admin? %>
    <div class="col-auto">
      <%= link_to "Rooms", department_rooms_path(@department), class: "btn btn-primary" %>
    </div>
  <% end %>
  <div class="col-auto">
    <%= link_to "Class", department_dep_classes_path(@department), class: "btn btn-primary" %>
  </div>
  <% if current_user.admin? %>
    <div class="col-auto">
      <%= link_to "Slots", department_slots_path(@department), class: "btn btn-primary" %>
    </div>
  <% end %>
  <div class="col-auto">
    <%= link_to "Department Time Table", department_dep_time_table_path(@department), class: "btn btn-primary" %>
  </div>

  </div>

  <h2 class="mt-4">Rooms Information</h2>
  <%= link_to "Add Room", new_department_room_path(params[:department_id]), class: "btn btn-primary" %>

  <table class="table mt-4">
    <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Room Name</th>
      <th scope="col">Department</th>
      <!--      <th scope="col">Teachers</th>-->
    </tr>
    </thead>
    <tbody>
    <% @department.rooms.all.each.with_index(1) do |room, index| %>
      <tr>
        <th scope="row"><%= index %></th>
        <td><%= room.name %></td>
        <td><%= room.department.name %></td>
        <!--        <td>-->
        <!--          <div class="ui selection dropdown">-->
        <!--            <i class="dropdown icon"></i>-->
        <!--            <div class="default text fs-5 ms-5">Teachers</div>-->
        <!--            <div class="menu scrolling">-->
        <%# course.teachers.each do |teacher| %>
        <!--                <div class="item" data-value="<%#= teacher.id %>"><%#= teacher.name.capitalize%></div>-->
        <%# end %>
        <!--            </div>-->
        <!--          </div>-->
        <!--        </td>-->
      </tr>
    <% end %>
    </tbody>
  </table>
</div>
