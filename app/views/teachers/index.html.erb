<p style="color: green"><%= notice %></p>

<h1>Teachers</h1>

<div id="teachers">
  <% @teachers.each do |teacher| %>
    <%= render teacher %>
    <p>
      <%= link_to "Show this teacher", department_course_path(teacher.department_id,teacher) %>
    </p>
  <% end %>
</div>

<%= link_to "New teacher", new_department_teacher_path %>
