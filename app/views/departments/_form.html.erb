<%= form_with(model: department) do |form| %>
  <% if department.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(department.errors.count, "error") %> prohibited this department from being saved:</h2>

      <ul>
        <% department.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <section class="">
  <div class="container">
    <div class="signup-content">
      <div class="signup-form">
        <h2 class="form-title">Create New Course</h2>
        <form method="POST" class="register-form" id="register-form">
          <div class="form-group">
            <label for="name"><i class="zmdi zmdi-account material-icons-name fs-5"></i></label>
            <div class="form-group">
                  <%#= form.label :courseName, style: "display: block" %>
                  <%= form.text_field :name,placeholder: "Department Name",class: "fs-5"%>
            </div>

           
            <div class="form-group form-button">
              <%= form.submit "Create Department",class: "form-submit fs-5" %>
            </div>


          </div>
        </form>
        <div>
          <%= link_to "Back to Departments", departments_path %>
        </div>
      </div>
    </div>
  </div>

</section>
<% end %>
